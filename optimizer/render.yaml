services:
  - type: web
    name: hydrogen-pipeline-planner
    env: python
    plan: free   # or starter/standard depending on load
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
